% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irrad_wl_amp.R
\name{measure_trace_window}
\alias{measure_trace_window}
\title{Measure one stimulus-response trace using windowed trough-to-peak logic}
\usage{
measure_trace_window(
  trace_df,
  baseline_window_ms = NULL,
  response_window_ms = NULL,
  awave_window_ms = NULL,
  stimulus_onset_ms = 400,
  time_reference = c("absolute", "stimulus"),
  trough_search_window_ms = NULL,
  peak_search_window_ms = NULL,
  dwave_baseline_window_ms = NULL,
  dwave_window_ms = NULL,
  dwave_trough_search_window_ms = NULL,
  dwave_peak_search_window_ms = NULL
)
}
\arguments{
\item{trace_df}{Single-trace data frame containing at least \code{time} and
\code{value}. If \code{time_reference = "stimulus"}, \code{time_rel_ms} may also be used
when available.}

\item{baseline_window_ms}{Numeric length-2 vector for the main baseline
window. If \code{NULL}, a default is chosen based on \code{time_reference}.}

\item{response_window_ms}{Numeric length-2 vector for the main response
window. If \code{NULL}, a default is chosen based on \code{time_reference}.}

\item{awave_window_ms}{Numeric length-2 vector for the A-wave search window.
If \code{NULL}, a default is chosen based on \code{time_reference}.}

\item{stimulus_onset_ms}{Numeric scalar used only for absolute-time fallback
when computing post-stimulus timing values. Default is \code{400}.}

\item{time_reference}{One of \code{"absolute"} or \code{"stimulus"}.}

\item{trough_search_window_ms}{Numeric length-2 vector for the main trough
search. If \code{NULL}, defaults to \code{response_window_ms}.}

\item{peak_search_window_ms}{Numeric length-2 vector for the main peak
search. If \code{NULL}, defaults to \code{response_window_ms}.}

\item{dwave_baseline_window_ms}{Numeric length-2 vector for the d-wave
baseline window. If \code{NULL}, a default is chosen based on \code{time_reference}.}

\item{dwave_window_ms}{Numeric length-2 vector for the overall d-wave window.
If \code{NULL}, a default is chosen based on \code{time_reference}.}

\item{dwave_trough_search_window_ms}{Numeric length-2 vector for the d-wave
trough search. If \code{NULL}, defaults to \code{dwave_window_ms}.}

\item{dwave_peak_search_window_ms}{Numeric length-2 vector for the d-wave
peak search. If \code{NULL}, defaults to \code{dwave_window_ms}.}
}
\value{
A one-row tibble containing trace-level measurements.
}
\description{
Computes baseline, response summary values, trough-to-peak amplitude, a-wave
estimate, and d-wave metrics from a single trace using configurable analysis
windows.
}
