% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_function.R
\name{zeus_plot_mean_waveform}
\alias{zeus_plot_mean_waveform}
\title{Plot mean ERG waveforms from ZEUS StimResp output}
\usage{
zeus_plot_mean_waveform(
  x,
  data_slot = c("traces_70", "traces_280"),
  channel_filter = NULL,
  compare_raw = FALSE,
  include_overall = TRUE,
  overlay_markers = FALSE,
  include_photocell = TRUE,
  photocell_filter = "Photocell",
  photocell_color = "black",
  photocell_auto_scale = TRUE,
  photocell_relative_height = 1.1,
  a_window = c(400, 700),
  b_window = c(400, 700),
  d_window = c(700, 1000),
  wavelength_select = "450",
  stim_levels = NULL,
  color_by = c("stim_nd", "stim_label", "wavelength"),
  overall_color = "red",
  marker_color = "black",
  base_size = 12
)
}
\arguments{
\item{x}{A \code{zeus_stimresp} object or a long-format waveform data frame.}

\item{data_slot}{For \code{zeus_stimresp} objects, which component to plot.
Default is \code{"traces_70"}. Can also be \code{"traces_280"}.}

\item{channel_filter}{Optional channel to filter. Ignored if the selected
data slot does not contain a \code{channel} column.}

\item{compare_raw}{Logical; if \code{TRUE}, plots both raw and smoothed means.
Requires \code{value_raw} in the plotted data. Default is \code{FALSE}.}

\item{include_overall}{Logical; if \code{TRUE}, includes an overall mean waveform
across all plotted traces.}

\item{overlay_markers}{Logical; if \code{TRUE}, overlays vertical reference lines
at the A-wave trough, B-wave peak, and D-wave peak locations derived from
the overall mean waveform.}

\item{include_photocell}{Logical; if \code{TRUE}, adds the mean photocell trace
when available.}

\item{photocell_filter}{Character string used to identify the photocell
channel when plotting from a data frame or from \code{traces_280}.}

\item{photocell_color}{Color for the photocell trace. Default \code{"black"}.}

\item{photocell_auto_scale}{Logical; if \code{TRUE} (default), rescales and
positions the photocell trace below the ERG signal following the Origin
StimResp convention: the pulse height equals
\code{photocell_relative_height * ERG_range} and the baseline sits slightly
below the minimum ERG value.}

\item{photocell_relative_height}{Fraction of the ERG amplitude range that
the photocell pulse should occupy when \code{photocell_auto_scale = TRUE}.
Default is \code{1.1} (110\% of the ERG range).}

\item{a_window}{Numeric length-2 vector giving the A-wave search interval in
milliseconds.}

\item{b_window}{Numeric length-2 vector giving the B-wave search interval in
milliseconds.}

\item{d_window}{Numeric length-2 vector giving the D-wave search interval in
milliseconds.}

\item{wavelength_select}{Optional character or numeric wavelength-block
selector for spectral C0 data. When supplied, the plot is filtered to the
matching stimulus-label prefix before averaging, so only the ND levels for
that wavelength block are shown. Examples include \code{"570"}, \code{"650A"}, and
\code{"650B"}. Default is \code{NULL}.}

\item{stim_levels}{Optional vector giving the desired order of stimulus labels
in the legend. If \code{NULL}, uses the order present in the data.}

\item{color_by}{One of \code{"stim_nd"}, \code{"stim_label"}, or \code{"wavelength"}.
Defaults to \code{"stim_nd"} so that traces with the same neutral-density level
are averaged together regardless of wavelength — matching the Origin
StimResp grouping for spectral (C0) protocols.  Use \code{"stim_label"} to keep
each wavelength × ND combination as a separate trace.}

\item{overall_color}{Color for the overall mean line.}

\item{marker_color}{Color for marker reference lines.}

\item{base_size}{Base font size for the plot theme.}
}
\value{
A ggplot object.
}
\description{
Visualizes averaged ERG waveforms from ZEUS StimResp output.

This function works with either:
\itemize{
\item a \code{zeus_stimresp} object returned by \code{build_stimresp()} or
\code{zeus_read_abf()}, or
\item a long-format data frame containing waveform data.
}

Stimulus-specific mean waveforms are drawn as thinner color-coded lines,
while the overall mean waveform is drawn as a thicker line.
Time is displayed in milliseconds.

If marker overlay is requested, the function computes reference lines from
the overall mean waveform itself so the lines align with the visible mean
trace. The reference lines correspond to:
\itemize{
\item A-wave trough
\item B-wave peak
\item D-wave peak
}
}
