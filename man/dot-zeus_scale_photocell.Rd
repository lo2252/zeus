% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_function.R
\name{.zeus_scale_photocell}
\alias{.zeus_scale_photocell}
\title{Scale and position a photocell trace for Origin-like display}
\usage{
.zeus_scale_photocell(
  df_photocell_source,
  photocell_filter = "Photocell",
  erg_ymin,
  erg_range,
  photocell_relative_height = 0.2
)
}
\arguments{
\item{df_photocell_source}{A data frame containing photocell data, expected
to have \code{time_ms} and \code{signal} columns.}

\item{photocell_filter}{Pattern passed to \code{\link[=grepl]{grepl()}} to identify the
photocell channel when a \code{channel} column is present.}

\item{erg_ymin}{Numeric. Minimum Y value of the ERG signal (used for
positioning).}

\item{erg_range}{Numeric. Total Y range of the ERG signal (used for
scaling).}

\item{photocell_relative_height}{Fraction of \code{erg_range} that the photocell
pulse should occupy. Default is \code{0.20}.}
}
\value{
A data frame with \code{time_ms} and \code{signal} columns ready to overlay
on a plot, or \code{NULL} if no valid photocell data could be prepared.
}
\description{
Internal helper used by ZEUS plotting functions to normalize a raw photocell
trace so that it appears below the ERG waveform — matching the display
convention used in Origin StimResp outputs.
}
\details{
The photocell is normalized to \code{[0, 1]}, scaled to occupy
\code{photocell_relative_height * erg_range} in Y units, and shifted so its
baseline sits just below \code{erg_ymin}.
}
\keyword{internal}
