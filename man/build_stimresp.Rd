% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stimresp.R
\name{build_stimresp}
\alias{build_stimresp}
\title{Build stimulus-response traces}
\usage{
build_stimresp(
  traces_280,
  stimresp_zero_baseline = TRUE,
  stimresp_baseline_window_ms = c(300, 400),
  stimresp_exclude_noisy = TRUE,
  stimresp_noise_window_ms = c(300, 1000),
  stimresp_noise_ratio_cutoff = 1.5,
  stimresp_runmean_k = 16L,
  stimresp_sg_smooth = TRUE,
  stimresp_sg_n = 101L,
  stimresp_sg_p = 2L
)
}
\arguments{
\item{traces_280}{A long replicate-level trace table containing at least
\code{sweep}, \code{stim_index}, \code{stim_label}, \code{time_ms}, and \code{value}.}

\item{stimresp_zero_baseline}{Logical; whether to baseline-correct each sweep
before averaging. Default is \code{TRUE}.}

\item{stimresp_baseline_window_ms}{Numeric length-2 vector giving the
baseline window in milliseconds. Default is \code{c(300, 400)}.}

\item{stimresp_exclude_noisy}{Logical; whether to exclude noisy technical
replicates before averaging. Default is \code{TRUE}.}

\item{stimresp_noise_window_ms}{Numeric length-2 vector giving the time
window in milliseconds used to estimate replicate-level standard deviation.
Default is \code{c(300, 1000)}.}

\item{stimresp_noise_ratio_cutoff}{Numeric cutoff applied to the within-group
SD ratio. Replicates with \code{sd_ratio >= stimresp_noise_ratio_cutoff} are
removed before averaging. Default is \code{1.5}.}

\item{stimresp_runmean_k}{Integer running-average width applied after
replicate averaging. Use \code{1L} for no running average. Default is \code{16L}.}

\item{stimresp_sg_smooth}{Logical; whether to apply Savitzky-Golay smoothing
after replicate averaging. Default is \code{TRUE}.}

\item{stimresp_sg_n}{Odd integer Savitzky-Golay window size. If even, it is
incremented to the next odd integer. Default is \code{101L}.}

\item{stimresp_sg_p}{Integer Savitzky-Golay polynomial order. Default is \code{2L}.}
}
\value{
A list with:
\describe{
\item{traces_70}{Averaged and optionally smoothed stimulus-response traces.}
\item{stimresp_qc}{Per-sweep noisy-replicate QC summary.}
}
}
\description{
Applies the standard StimResp pipeline to labeled replicate-level ERG traces:
optional per-sweep baseline correction, optional noisy-replicate exclusion,
replicate averaging, and optional post-averaging smoothing.
}
\keyword{internal}
