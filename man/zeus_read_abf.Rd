% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ERG_Import.R
\name{zeus_read_abf}
\alias{zeus_read_abf}
\title{Read an ABF file and build a ZEUS stimulus-response object}
\usage{
zeus_read_abf(
  path,
  protocol = c("C0", "C1"),
  erg_channel = "ERG DAM80",
  pc_channel = "Photocell",
  zero_baseline = FALSE,
  smooth_n = 1L,
  align_to_stimulus = c("protocol", "photocell"),
  stimresp_zero_baseline = TRUE,
  stimresp_baseline_window_ms = c(300, 400),
  stimresp_exclude_noisy = TRUE,
  stimresp_noise_window_ms = c(300, 1000),
  stimresp_noise_ratio_cutoff = 1.5,
  stimresp_runmean_k = 16L,
  stimresp_drift_correct = FALSE,
  stimresp_drift_tail_n = 100L,
  stimresp_sg_smooth = TRUE,
  stimresp_sg_n = 101L,
  stimresp_sg_p = 2L,
  ...
)
}
\arguments{
\item{path}{File path to an ABF file.}

\item{protocol}{Protocol id. One of \code{"C0"} or \code{"C1"}.}

\item{erg_channel}{Name of the ERG channel in the ABF file. Default is
\code{"ERG DAM80"}.}

\item{pc_channel}{Name of the photocell channel in the ABF file. Default is
\code{"Photocell"}.}

\item{zero_baseline}{Logical; apply baseline subtraction at the
technical-replicate level before averaging. Default is \code{FALSE}.}

\item{smooth_n}{Integer running-mean window size applied to replicate-level
traces before any downstream processing. Default is \code{1L}.}

\item{align_to_stimulus}{One of \code{"protocol"} or \code{"photocell"}.}

\item{stimresp_zero_baseline}{Logical; apply baseline subtraction to
replicate-level traces before averaging into stimulus-response traces.
Default is \code{TRUE}.}

\item{stimresp_baseline_window_ms}{Numeric length-2 vector for baseline
subtraction. Default is \code{c(300, 400)}.}

\item{stimresp_exclude_noisy}{Logical; exclude noisy technical replicates
before averaging. Default is \code{TRUE}.}

\item{stimresp_noise_window_ms}{Numeric length-2 vector for replicate-level
noise estimation. Default is \code{c(300, 1000)}.}

\item{stimresp_noise_ratio_cutoff}{Numeric SD-ratio threshold used to remove
noisy technical replicates. Default is \code{1.5}.}

\item{stimresp_runmean_k}{Integer running-average window size for averaged
stimulus-response traces. Use \code{1L} for none. Default is \code{16L}.}

\item{stimresp_drift_correct}{Logical; stored in the output object for
downstream workflows. Default is \code{FALSE}.}

\item{stimresp_drift_tail_n}{Integer number of final points used for
downstream drift estimation. Default is \code{100L}.}

\item{stimresp_sg_smooth}{Logical; apply Savitzky-Golay smoothing to averaged
stimulus-response traces. Default is \code{TRUE}.}

\item{stimresp_sg_n}{Integer Savitzky-Golay window size for averaged traces.
Default is \code{101L}.}

\item{stimresp_sg_p}{Integer Savitzky-Golay polynomial order. Default is \code{2L}.}

\item{...}{Additional arguments passed to \code{\link[=read_abf_raw]{read_abf_raw()}}.}
}
\value{
An object of class \code{zeus_stimresp}.
}
\description{
Wrapper around \code{\link[=build_stimresp]{build_stimresp()}} that reads an ABF file, extracts ERG and
optional photocell channels, labels stimulus structure, and returns a
structured \code{zeus_stimresp} object containing raw, replicate-level, and
averaged stimulus-response outputs.
}
