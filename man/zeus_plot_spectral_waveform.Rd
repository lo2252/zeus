% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_function.R
\name{zeus_plot_spectral_waveform}
\alias{zeus_plot_spectral_waveform}
\title{Plot ERG waveforms split into per-wavelength-block facets}
\usage{
zeus_plot_spectral_waveform(
  x,
  data_slot = c("traces_70", "traces_280"),
  channel_filter = NULL,
  stim_levels = NULL,
  include_photocell = TRUE,
  photocell_filter = "Photocell",
  photocell_color = "black",
  photocell_relative_height = 0.2,
  protocol_label = NULL,
  file_label = NULL,
  facet_ncol = 5L,
  base_size = 10
)
}
\arguments{
\item{x}{A \code{zeus_stimresp} object (output of \code{\link[=zeus_read_abf]{zeus_read_abf()}}) or a
long-format waveform data frame containing at least \code{time}, \code{value},
\code{stim_label}, \code{stim_nd}, and \code{block_index}.}

\item{data_slot}{For \code{zeus_stimresp} objects, which component to use.
Default is \code{"traces_70"} (averaged traces). Can also be \code{"traces_280"}.}

\item{channel_filter}{Optional channel to filter. Ignored when the selected
data slot has no \code{channel} column.}

\item{stim_levels}{Optional numeric vector giving the desired ND levels to
display and their legend order (highest = dimmest first).  If \code{NULL},
all ND levels present in the data are used in descending order.}

\item{include_photocell}{Logical; if \code{TRUE} (default), overlays the mean
photocell trace on every panel when photocell data are available.}

\item{photocell_filter}{Character pattern used to identify the photocell
channel when a \code{channel} column is present. Default is \code{"Photocell"}.}

\item{photocell_color}{Color for the photocell overlay line. Default
\code{"black"}.}

\item{photocell_relative_height}{Fraction of the global ERG amplitude range
that the photocell pulse occupies. Default is \code{0.20}.}

\item{protocol_label}{Character string shown in every panel label to
identify the recording protocol (e.g. \code{"C0"} or \code{"C1"}). When \code{x} is a
\code{zeus_stimresp} object this is auto-detected from \code{x$protocol}; supply
this argument to override or when passing a plain data frame.}

\item{file_label}{Character string shown in every panel label to identify
the source file (the \code{.abf} extension is excluded). When \code{x} is a
\code{zeus_stimresp} object this is auto-detected from \code{x$path}; supply this
argument to override or when passing a plain data frame.}

\item{facet_ncol}{Number of columns in the facet grid. Default is \code{5}.}

\item{base_size}{Base font size for the plot theme. Default is \code{10}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Generates a publication-ready multi-panel waveform plot in which each panel
corresponds to one wavelength block of the C0 (spectral) protocol — or, for
single-wavelength (C1/white-light) data, one run block.  Within each panel,
individual traces for each neutral-density (ND) level are drawn and
color-coded using a perceptually uniform sequential palette (lightest = most
attenuated / highest ND; darkest = least attenuated / lowest ND).

This layout mirrors the Origin "spectral waveform" panel: the 10 wavelength
blocks form a 2-row × 5-column grid. When photocell data are available a
representative photocell pulse is overlaid at the bottom of every panel,
positioned and scaled to match the Origin StimResp display convention.

The time scale is shown underneath every individual panel (requires
ggplot2 >= 3.4.0). Each panel's strip label is composed of two lines:
\enumerate{
\item The wavelength prefix (C0) or block number (C1).
\item A metadata tag of the form \code{"Block N | C0/C1 | filename"}.
}
When \code{x} is a \code{zeus_stimresp} object the protocol and filename are
auto-detected; they can also be supplied explicitly via \code{protocol_label}
and \code{file_label}.
}
