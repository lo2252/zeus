% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_function.R
\name{zeus_plot_intensity_response}
\alias{zeus_plot_intensity_response}
\title{Plot ERG intensity-response curves for A-, B-, and D-wave features}
\usage{
zeus_plot_intensity_response(
  x,
  group_col = NULL,
  use_se = TRUE,
  base_size = 11
)
}
\arguments{
\item{x}{A \code{zeus_stimresp} object (output of \code{\link[=zeus_read_abf]{zeus_read_abf()}}).}

\item{group_col}{Optional character string specifying a grouping variable
present in \code{x$traces_70} (e.g., \code{"treatment_group"}). If provided,
separate curves are plotted for each group.}

\item{use_se}{Logical; if \code{TRUE}, standard error bars (mean ± SE) are
displayed. Default is \code{TRUE}.}

\item{base_size}{Numeric base font size for the plot theme. Default is 11.}
}
\value{
A \code{ggplot2} object showing intensity-response curves for each
wave type.
}
\description{
This function generates a multi-panel intensity-response plot for ERG data,
displaying A-wave trough amplitude, B-wave peak amplitude, and D-wave peak
amplitude as a function of stimulus ND.

The function operates on a \code{zeus_stimresp} object returned by
\code{\link[=zeus_read_abf]{zeus_read_abf()}}, extracting per-trace waveform measurements via
\code{\link[=extract_irrad_wl_amp]{extract_irrad_wl_amp()}} before computing summary statistics.

The resulting plot shows mean response values for each wave type across
stimulus ND levels, optionally grouped by a categorical variable such as
treatment group. Standard error bars can also be included.
}
\details{
The three panels correspond to:
\itemize{
\item A-wave trough amplitude (\code{awave_mv})
\item B-wave peak amplitude (\code{amp_mv})
\item D-wave peak amplitude (\code{dwave_mv})
}

Stimulus ND is displayed on the x-axis in descending order so that larger ND
values (dimmer stimuli) appear first.
}
